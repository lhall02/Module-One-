<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>{{title}}</title>
  <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
  <div id="background">
    <div id="page">
      {{> header}}

      <div id="contents">
        <div class="box">
          <div>
            <div class="body">
              <h1>Travel</h1>
              <ul id="sites">
                {{#each trips}}
                  <li>
                    <a href="/travel/{{code}}"><img src="{{image}}" alt="{{name}}"></a>
                    <h2><a href="/travel/{{code}}">{{name}}</a></h2>

                    <p>{{description}}</p>

                    <p>
                      <strong>Trip Code:</strong> {{code}}<br>
                      <strong>Length:</strong> {{length}}<br>
                      <strong>Start:</strong> {{start}}<br>
                      <strong>Resort:</strong> {{resort}}<br>
                      <strong>Price Per Person:</strong> ${{perPerson}}
                    </p>
                    <p class="trip-actions">
                      <a class="button-link" href="/travel/{{code}}">View Details</a>
                    </p>
                  </li>
                {{/each}}
              </ul>

              {{#unless trips.length}}
                <p>No trips available right now.</p>
              {{/unless}}
            </div>
          </div>
        </div>
      </div>
    </div>

    {{> footer}}
  </div>
</body>
</html>
